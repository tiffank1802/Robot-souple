function [B] = Bconstruct(M,C,K,nA,dt,beta,gamma)
nx=size(M,1);
u0=zeros(nx,1);
v0=zeros(nx,1);
a0=zeros(nx,1);
eta=zeros(nx,1);
eta(nA)=1;
u1=zeros(nx)
[u,v,a]=newmark1stepMRHS(M,C,K,eta,u0,v0,a0,dt,beta,gamma);
    
end